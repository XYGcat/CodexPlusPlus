# 2026-06-01 变更记录

## 一、新增 Claude Code 项目配置

### CLAUDE.md

新增项目知识文件，包含架构概览、常用命令、开发约定、平台说明和测试指南。每次 Claude Code 会话自动加载。

### Hooks（自动钩子）

`.claude/settings.json` 中配置了两条 PostToolUse 钩子：

| 钩子 | 触发条件 | 行为 |
|---|---|---|
| `auto-format.sh` | Edit/Write 工具执行后 | `.rs` 文件运行 `cargo fmt`；`.ts/.tsx` 文件运行 `prettier` |
| `auto-test.sh` | Edit/Write `.rs` 文件后 | 根据文件路径自动运行对应 crate 的 `cargo test -p <crate>` |

### Skills（技能）

| 技能 | 路径 | 调用方式 | 功能 |
|---|---|---|---|
| `/commit` | `.claude/skills/commit/SKILL.md` | 用户调用 | 遵循项目规范（`fix`/`feat`/`docs`/`refactor` 前缀）创建 git commit |
| `/pr-check` | `.claude/skills/pr-check/SKILL.md` | 用户调用 | PR 前全量验证：`cargo xcheck` → `cargo clippy` → `cargo xtest` → 前端构建 |

### Agents（子代理）

| 子代理 | 路径 | 功能 |
|---|---|---|
| `code-reviewer` | `.claude/agents/code-reviewer.md` | Rust/Tauri 代码审查：正确性、异步安全、SQLite 事务、跨平台兼容 |
| `security-reviewer` | `.claude/agents/security-reviewer.md` | 安全审计：CDP 注入、凭据处理、bridge 信任边界、供应链风险 |

### MCP 服务器

| 服务器 | 状态 | 用途 |
|---|---|---|
| `context7` | 已连接 | 实时库文档查询（tokio、reqwest、React 等） |
| `github` | 已连接 | GitHub Issue/PR 管理（需认证后解锁完整功能） |

### 插件

| 插件 | 状态 |
|---|---|
| `frontend-design` | 已安装（用户级） |
| `hookify` | 已安装（用户级） |
| `feature-dev` | 已安装（用户级） |

### Hookify 安全规则（用户级）

以下规则位于 `~/.claude/`，对所有项目生效，触发时阻止操作：

| 规则文件 | 触发场景 |
|---|---|
| `hookify.block-rm-dangerous.local.md` | `rm -rf /`、`rm -rf ~` 等对系统关键路径的强制删除 |
| `hookify.block-destructive-git.local.md` | `push --force`、`reset --hard`、`clean -fd` 等不可逆 git 操作 |
| `hookify.block-sensitive-files.local.md` | 编辑 `.env`、`auth.json`、`.pem`、`.key` 等敏感文件 |
| `hookify.block-privilege-escalation.local.md` | `sudo`、`chmod 777`、`systemctl` 等提权命令 |
| `hookify.block-outside-workspace.local.md` | 编辑项目工作目录外的文件 |

### .gitignore 变更

将 `.claude/` 整目录忽略改为精确忽略，使项目级配置可被提交：

```
.claude/worktrees/
.claude/settings.local.json
.claude/*.local.md
.claude/scheduled_tasks.json
```

---

## 二、删除推荐内容（ads）功能

移除了整个推荐内容/广告系统，涉及 6 层代码、11 个文件。

### 删除的文件

| 文件 | 说明 |
|---|---|
| `crates/codex-plus-core/src/ads.rs` | 广告获取模块（65 行）：URL 常量、缓存清除、远端拉取、数据规范化 |
| `crates/codex-plus-core/tests/ads.rs` | 广告模块测试（121 行）：URL 验证、缓存清除、规范化、故障转移 |

### 修改的文件

| 文件 | 删除内容 |
|---|---|
| `crates/codex-plus-core/src/lib.rs` | `pub mod ads;` 模块声明 |
| `crates/codex-plus-core/src/routes.rs` | `BridgeRuntimeService` trait 中的 `ads()` 方法；`handle_bridge_request` 中 `/ads` 路由分发；`CoreRuntimeService` 中 `ads()` 默认实现 |
| `crates/codex-plus-core/tests/bridge_routes.rs` | `/ads` 测试用例；mock runtime 中的 `ads()` 实现 |
| `apps/codex-plus-launcher/src/main.rs` | `LauncherRuntimeService` 中 `ads()` trait 实现 |
| `apps/codex-plus-manager/src-tauri/src/commands.rs` | `AdsPayload` 结构体；`load_ads` Tauri 命令；`ads_payload` 辅助函数；`ads_payload_keeps_version_and_ad_items` 单元测试 |
| `apps/codex-plus-manager/src-tauri/src/lib.rs` | `load_ads` 命令注册 |
| `apps/codex-plus-manager/src/App.tsx` | `AdItem`、`AdsResult` 类型定义；`recommendations` 路由及侧边栏菜单项；`ads` 状态和 `refreshAds` 函数；`RecommendationsScreen`、`AdGrid` 组件；`isExpiredAd` 辅助函数；路由子标题 |
| `apps/codex-plus-manager/src/styles.css` | `.recommend-hero`、`.ad-grid`、`.ad-card`、`.ad-tags`、`.ad-link` 全部样式（88 行） |
| `assets/inject/renderer-inject.js` | 广告相关 CSS（12 条规则）；6 个 JS 函数（`isCodexPlusAdExpired`、`normalizeCodexPlusAds`、`renderCodexPlusAdGroup`、`renderCodexPlusAds`、`cacheBustCodexPlusAdUrl`、`directFetchCodexPlusAds`、`fetchCodexPlusAds`）；状态变量；sponsor 面板 HTML；"推荐内容" tab 按钮；modal 打开时的 fetch 触发 |
| `README.md` | "推荐内容"章节（12 行）；增强功能段中"推荐内容"提及 |
| `README_EN.md` | "Recommendations"章节（12 行）；enhancements 段中 "recommendations" 提及 |

### 原数据流（已移除）

```
远端 ad list (GitHub + jsDelivr CDN)
    ↓ HTTP GET (cache-busted)
codex-plus-core/src/ads.rs (fetch_ad_list)
    ↓
routes.rs /ads bridge route
    ↓
Tauri load_ads command
    ↓
App.tsx RecommendationsScreen + AdGrid
```

### 验证

- Grep 扫描全部 `.rs`、`.tsx`、`.ts`、`.js`、`.md` 文件，零残留引用
- 唯一保留的 "recommendations" 出现在 `CONTRIBUTING.md:66`（clippy linting 建议，与 ads 无关）

---

## 三、删除供应商配置（relay）功能

移除了整个供应商配置/中继注入系统，涉及 6 层代码，总计约 17000+ 行。

### 删除的文件

| 文件 | 说明 |
|---|---|
| `crates/codex-plus-core/src/relay_config.rs` | 中继配置引擎（2065 行）：配置应用、清除、回填、测试、上下文管理 |
| `crates/codex-plus-core/src/protocol_proxy.rs` | 协议转换（3650 行）：Chat Completions ↔ Responses API 双向转换 |
| `crates/codex-plus-core/tests/relay_config.rs` | 中继配置测试（2270 行）：40+ 测试覆盖全部配置操作 |
| `crates/codex-plus-core/tests/protocol_proxy.rs` | 协议代理测试 |

### 修改的文件

| 文件 | 删除内容 |
|---|---|
| `crates/codex-plus-core/src/lib.rs` | `pub mod relay_config;` 和 `pub mod protocol_proxy;` 模块声明 |
| `crates/codex-plus-core/src/settings.rs` | 6 个 relay 类型（LaunchMode、RelayProfile、RelayContextSelection、RelayModelInsertMode、RelayProtocol、RelayMode）；BackendSettings 中 9 个 relay 字段；active_relay_profile() 方法；4 个默认值函数；10 个测试 |
| `crates/codex-plus-core/src/launcher.rs` | apply_active_relay_profile() trait 方法及实现；relay_protocol_proxy_enabled()；3 个协议代理处理器 |
| `crates/codex-plus-core/src/model_catalog.rs` | relay_profile_model_catalog_value()、relay_profile_model_ids()、fetch_relay_profile_model_ids() |
| `crates/codex-plus-core/src/ccs_import.rs` | 所有 relay 类型引用；relay_profile_from_ccs() 等 8 个函数 |
| `crates/codex-plus-core/src/paths.rs` | 新增 default_codex_home_dir()（从已删除的 relay_config 迁移） |
| `crates/codex-plus-core/tests/bridge_routes.rs` | 测试中的 relay 配置字段 |
| `crates/codex-plus-core/tests/launcher.rs` | 5 个 relay 专用测试 |
| `crates/codex-plus-core/tests/model_catalog.rs` | relay 模型目录测试 |
| `crates/codex-plus-core/tests/cdp_bridge.rs` | 2 个 relay 模式测试 |
| `apps/codex-plus-launcher/src/main.rs` | apply_active_relay_profile() 实现 |
| `apps/codex-plus-manager/src-tauri/src/commands.rs` | 13 个结构体、24 个函数、15 个 Tauri 命令、11 个测试（~1400 行） |
| `apps/codex-plus-manager/src-tauri/src/lib.rs` | 15 个 relay 命令注册 |
| `apps/codex-plus-manager/src-tauri/tests/windows_subsystem.rs` | 4 个 relay 测试 |
| `apps/codex-plus-manager/src/App.tsx` | 全部 relay 类型、状态、函数、组件（RelayScreen、RelayProfileList 等）；从 4626 行减至 2627 行（-1999 行） |
| `apps/codex-plus-manager/src/styles.css` | 全部 `.relay-*` CSS 类和动画（~900 行） |
| `assets/inject/renderer-inject.js` | relayPatchDisabled 变量、pluginPatchDisabledInRelayMode() 函数、relay 条件文本、data-relay-unneeded 属性 |
| `CLAUDE.md` | 架构表中 "relay proxy" 提及 |
| `README_EN.md` | "Relay Injection" 整个章节（25 行）；功能亮点中的 relay 行；增强功能中的 relay 引用 |

### 原数据流（已移除）

```
用户配置 (RelayProfile)
    ↓
relay_config.rs (配置应用/清除/回填)
    ↓ ~/.codex/config.toml + auth.json
protocol_proxy.rs (Chat Completions ↔ Responses 转换)
    ↓ HTTP 代理 :57321/v1
Codex App (使用转换后的 API)
```

### 验证

- Grep 扫描全部源码、测试、前端、CSS、文档
- 功能代码零残留
- 唯一保留的 "relay" 出现在：赞助商描述（描述赞助商的中继服务）和 ccs_import 测试中的 `relay.example` 域名
